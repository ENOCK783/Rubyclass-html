
const colorButton ;HTMLElement =document.getElementById( elementid: "color button");

colorButton.addEventListener(type='click',listener:() :void=>)

    colorButton.style.backgroundColor=colorButton.style.backgroundColor==="blue" ? "blue";